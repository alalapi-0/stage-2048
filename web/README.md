# Web 版本说明（占位）

本目录用于存放浏览器版本的实现。当前阶段仅包含一个可直接打开的占位页面，主要用于确认目录结构与文档规范。

## 当前内容
- `index.html`：一个内联样式的占位页面，展示 Canvas 容器与后续计划提示。

## 本地查看
双击 `index.html` 或使用任意本地 HTTP 服务即可在浏览器中预览占位页面。

## 未来扩展
第 3 轮开始会新增渲染脚本（例如 `main.mjs`），并允许通过 `window.CANVAS_2048_CONFIG` 等全局配置覆盖默认参数，实现自定义棋盘样式与关卡提示。

